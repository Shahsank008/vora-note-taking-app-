/* ===== RESET & BASE ===== */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

:root {
    /* Samsung One UI inspired palette */
    --bg:               #F5F5F7;
    --surface:          #FFFFFF;
    --surface-hover:    #F0F0F2;
    --surface-active:   #E8E8EC;
    --text:             #1D1D1F;
    --text-secondary:   #86868B;
    --text-tertiary:    #AEAEB2;
    --border:           #E5E5EA;
    --accent:           #1476F2;
    --accent-light:     #E8F1FE;
    --danger:           #FF3B30;
    --danger-light:     #FFEBEA;
    --star:             #FFBF00;
    --sidebar-bg:       #FFFFFF;
    --sidebar-w:        280px;
    --topbar-h:         64px;
    --fab-size:         60px;
    --radius:           14px;
    --radius-sm:        10px;
    --shadow-sm:        0 1px 3px rgba(0,0,0,.06);
    --shadow-md:        0 4px 16px rgba(0,0,0,.08);
    --shadow-lg:        0 12px 40px rgba(0,0,0,.12);
    --transition:       .22s cubic-bezier(.4,0,.2,1);
    --font:             'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

/* Dark theme */
[data-theme="dark"] {
    --bg:               #111113;
    --surface:          #1C1C1E;
    --surface-hover:    #2C2C2E;
    --surface-active:   #3A3A3C;
    --text:             #F5F5F7;
    --text-secondary:   #8E8E93;
    --text-tertiary:    #636366;
    --border:           #2C2C2E;
    --accent:           #409CFF;
    --accent-light:     #1A2A40;
    --danger-light:     #3A1A1A;
    --sidebar-bg:       #1C1C1E;
    --shadow-sm:        0 1px 3px rgba(0,0,0,.2);
    --shadow-md:        0 4px 16px rgba(0,0,0,.3);
    --shadow-lg:        0 12px 40px rgba(0,0,0,.4);
}

html { font-size: 16px; }
body {
    font-family: var(--font);
    background: var(--bg);
    color: var(--text);
    display: flex;
    height: 100vh;
    overflow: hidden;
    transition: background var(--transition), color var(--transition);
    -webkit-font-smoothing: antialiased;
}

/* ===== SCROLLBAR ===== */
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: var(--text-tertiary); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: var(--text-secondary); }

/* ===== ICON BUTTON ===== */
.icon-btn {
    width: 40px; height: 40px;
    display: flex; align-items: center; justify-content: center;
    border: none; background: transparent; color: var(--text);
    border-radius: 50%; cursor: pointer;
    transition: background var(--transition), transform .15s;
    flex-shrink: 0;
}
.icon-btn:hover { background: var(--surface-hover); }
.icon-btn:active { background: var(--surface-active); transform: scale(.92); }
.icon-btn svg { width: 22px; height: 22px; }

/* ===== SIDEBAR ===== */
.sidebar {
    width: var(--sidebar-w);
    background: var(--sidebar-bg);
    border-right: 1px solid var(--border);
    display: flex; flex-direction: column;
    flex-shrink: 0;
    transition: transform var(--transition), background var(--transition), border-color var(--transition);
    z-index: 100;
    position: relative;
}
.sidebar.collapsed { transform: translateX(-100%); position: absolute; height: 100%; }

.sidebar-header {
    display: flex; align-items: center; justify-content: space-between;
    padding: 20px 16px 12px;
}
.sidebar-brand {
    display: flex; align-items: center; gap: 10px;
    font-size: 1.25rem; font-weight: 700; letter-spacing: -.02em;
}
.brand-icon { width: 28px; height: 28px; color: var(--accent); }
.sidebar-close-btn { display: none; }

.sidebar-nav {
    flex: 1; overflow-y: auto; padding: 4px 10px;
}
.nav-item {
    display: flex; align-items: center; gap: 12px;
    padding: 11px 14px; border-radius: var(--radius-sm);
    cursor: pointer; font-size: .9rem; font-weight: 500;
    color: var(--text-secondary);
    transition: background var(--transition), color var(--transition);
    user-select: none; position: relative;
}
.nav-item svg { width: 20px; height: 20px; flex-shrink: 0; }
.nav-item:hover { background: var(--surface-hover); color: var(--text); }
.nav-item.active {
    background: var(--accent-light); color: var(--accent); font-weight: 600;
}
.nav-badge {
    margin-left: auto; font-size: .75rem; font-weight: 600;
    color: var(--text-tertiary); min-width: 20px; text-align: center;
}
.nav-item.active .nav-badge { color: var(--accent); }
.cat-dot { width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; }
.nav-divider { height: 1px; background: var(--border); margin: 8px 14px; }
.nav-section-title {
    font-size: .7rem; font-weight: 700; letter-spacing: .08em;
    color: var(--text-tertiary); padding: 12px 14px 4px;
}

.sidebar-footer {
    display: flex; align-items: center; gap: 4px;
    padding: 12px 16px; border-top: 1px solid var(--border);
}

/* Theme icons */
[data-theme="dark"] #iconSun  { display: block; }
[data-theme="dark"] #iconMoon { display: none; }
#iconSun  { display: none; }
#iconMoon { display: block; }

/* Download APK Button in sidebar */
.download-apk-btn {
    margin-left: auto;
    display: flex; align-items: center; gap: 6px;
    padding: 7px 14px;
    border-radius: 20px;
    border: none;
    background: linear-gradient(135deg, #1476F2, #0D5DC7);
    color: #fff;
    font-size: .78rem;
    font-weight: 600;
    font-family: var(--font);
    cursor: pointer;
    transition: transform .15s, box-shadow var(--transition);
    box-shadow: 0 2px 8px rgba(20,118,242,.3);
    text-decoration: none;
}
.download-apk-btn:hover { transform: translateY(-1px); box-shadow: 0 4px 14px rgba(20,118,242,.4); }
.download-apk-btn:active { transform: scale(.96); }
.download-apk-btn svg { width: 15px; height: 15px; }

/* ===== MAIN AREA ===== */
.main-area {
    flex: 1; display: flex; flex-direction: column;
    position: relative; min-width: 0;
    transition: background var(--transition);
}

/* Topbar */
.topbar {
    height: var(--topbar-h); display: flex; align-items: center;
    padding: 0 20px; gap: 12px;
    border-bottom: 1px solid var(--border);
    background: var(--surface);
    flex-shrink: 0;
    transition: background var(--transition), border-color var(--transition);
}
.topbar-left { display: flex; align-items: center; gap: 8px; }
#hamburgerBtn { display: none; }
.topbar-title { font-size: 1.15rem; font-weight: 700; white-space: nowrap; }
.topbar-center { flex: 1; display: flex; justify-content: center; max-width: 480px; margin: 0 auto; }
.topbar-right { display: flex; align-items: center; gap: 4px; }

/* Search */
.search-box {
    position: relative; width: 100%;
}
.search-box input {
    width: 100%; padding: 10px 14px 10px 40px;
    border: 1px solid var(--border); border-radius: 12px;
    background: var(--bg); color: var(--text);
    font-size: .9rem; font-family: var(--font);
    transition: border-color var(--transition), background var(--transition), box-shadow var(--transition);
    outline: none;
}
.search-box input:focus { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(20,118,242,.15); }
.search-box input::placeholder { color: var(--text-tertiary); }
.search-icon {
    position: absolute; left: 12px; top: 50%; transform: translateY(-50%);
    width: 18px; height: 18px; color: var(--text-tertiary); pointer-events: none;
}

/* View toggle icons */
.grid-view ~ .topbar #iconList,
.list-view ~ .topbar #iconGrid { display: block; }
.grid-view ~ .topbar #iconGrid,
.list-view ~ .topbar #iconList { display: none; }
#iconList { display: none; }

/* ===== NOTES CONTAINER ===== */
.notes-container {
    flex: 1; overflow-y: auto; padding: 20px;
}

/* Grid View */
.notes-container.grid-view {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 14px; align-content: start;
}

/* List View */
.notes-container.list-view {
    display: flex; flex-direction: column; gap: 6px;
}

/* Note Card — Grid */
.note-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 18px;
    cursor: pointer;
    transition: transform .18s, box-shadow var(--transition), border-color var(--transition), background var(--transition);
    display: flex; flex-direction: column;
    min-height: 140px;
    position: relative;
    overflow: hidden;
    animation: cardIn .3s ease backwards;
}
@keyframes cardIn {
    from { opacity: 0; transform: translateY(12px) scale(.97); }
}
.note-card:hover {
    transform: translateY(-3px);
    box-shadow: var(--shadow-md);
    border-color: var(--accent);
}
.note-card:active { transform: scale(.98); }

.note-card .card-star {
    position: absolute; top: 12px; right: 12px;
    color: var(--star); width: 16px; height: 16px;
}
.note-card .card-category {
    width: 8px; height: 8px; border-radius: 50%;
    position: absolute; top: 14px; right: 14px;
}
.note-card.starred .card-category { right: 34px; }

.note-card .card-title {
    font-size: .95rem; font-weight: 650; line-height: 1.3;
    margin-bottom: 8px;
    display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden;
    padding-right: 28px;
}
.note-card .card-preview {
    font-size: .82rem; color: var(--text-secondary); line-height: 1.5;
    flex: 1;
    display: -webkit-box; -webkit-line-clamp: 4; -webkit-box-orient: vertical; overflow: hidden;
}
.note-card .card-footer {
    display: flex; align-items: center; justify-content: space-between;
    margin-top: 12px; font-size: .72rem; color: var(--text-tertiary);
}
.note-card .card-attachments {
    display: flex; align-items: center; gap: 4px;
    color: var(--accent); font-weight: 600;
}
.note-card .card-attachments svg { width: 13px; height: 13px; }

/* List mode overrides */
.list-view .note-card {
    flex-direction: row; align-items: center;
    min-height: unset; padding: 14px 18px; gap: 16px;
}
.list-view .note-card:hover { transform: translateX(4px); }
.list-view .note-card .card-title { margin-bottom: 0; flex-shrink: 0; min-width: 160px; max-width: 240px; -webkit-line-clamp: 1; }
.list-view .note-card .card-preview { -webkit-line-clamp: 1; flex: 1; }
.list-view .note-card .card-footer { margin-top: 0; flex-shrink: 0; }
.list-view .note-card .card-star { position: static; }

/* Color tinted cards */
.note-card[data-color] { border: none; }

/* ===== EMPTY STATE ===== */
.empty-state {
    position: absolute; inset: 0; top: var(--topbar-h);
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    color: var(--text-tertiary);
    pointer-events: none;
}
.empty-state.hidden { display: none; }
.empty-state svg { width: 80px; height: 80px; margin-bottom: 16px; opacity: .4; }
.empty-title { font-size: 1.2rem; font-weight: 600; margin-bottom: 6px; }
.empty-sub { font-size: .9rem; }

/* ===== FAB ===== */
.fab {
    position: fixed; bottom: 28px; right: 28px;
    width: var(--fab-size); height: var(--fab-size);
    border-radius: 50%; border: none;
    background: linear-gradient(135deg, #1476F2, #0D5DC7);
    color: #fff; cursor: pointer;
    box-shadow: 0 6px 24px rgba(20,118,242,.35);
    display: flex; align-items: center; justify-content: center;
    transition: transform .2s, box-shadow .2s;
    z-index: 90;
}
.fab svg { width: 28px; height: 28px; }
.fab:hover { transform: scale(1.08); box-shadow: 0 8px 32px rgba(20,118,242,.45); }
.fab:active { transform: scale(.94); }
.fab.open { transform: rotate(45deg); }

/* FAB Menu */
.fab-menu {
    position: fixed; bottom: 100px; right: 24px;
    display: flex; flex-direction: column; gap: 8px;
    z-index: 89;
    animation: fabMenuIn .2s ease;
}
.fab-menu.hidden { display: none; }
@keyframes fabMenuIn { from { opacity: 0; transform: translateY(10px); } }
.fab-option {
    display: flex; align-items: center; gap: 10px;
    padding: 10px 18px 10px 14px;
    background: var(--surface); border: 1px solid var(--border);
    border-radius: 28px; cursor: pointer;
    font-size: .88rem; font-weight: 600; font-family: var(--font);
    color: var(--text);
    box-shadow: var(--shadow-md);
    transition: transform .15s, background var(--transition);
    white-space: nowrap;
}
.fab-option svg { width: 20px; height: 20px; color: var(--accent); }
.fab-option:hover { background: var(--surface-hover); transform: translateX(-4px); }

/* ===== EDITOR OVERLAY ===== */
.editor-overlay {
    position: fixed; inset: 0; z-index: 200;
    background: rgba(0,0,0,.35);
    backdrop-filter: blur(6px);
    display: flex; justify-content: center; align-items: stretch;
    opacity: 0; pointer-events: none;
    transition: opacity .25s;
}
.editor-overlay.open { opacity: 1; pointer-events: auto; }
.editor-overlay.open .editor-panel { transform: translateY(0); }

.editor-panel {
    width: 100%; max-width: 860px;
    background: var(--surface);
    display: flex; flex-direction: column;
    margin: 16px; border-radius: var(--radius);
    box-shadow: var(--shadow-lg);
    transform: translateY(20px);
    transition: transform .3s cubic-bezier(.32,1.2,.5,1), background var(--transition);
    overflow: hidden;
}

.editor-topbar {
    display: flex; align-items: center; justify-content: space-between;
    padding: 8px 12px; border-bottom: 1px solid var(--border);
    flex-shrink: 0;
}
.editor-topbar-actions { display: flex; align-items: center; gap: 2px; }

/* Star icons */
#starFilled { display: none; color: var(--star); }
.starred #starOutline { display: none; }
.starred #starFilled { display: block; }

/* Editor Menu */
.editor-menu-wrapper { position: relative; }
.dropdown-menu {
    position: absolute; top: 100%; right: 0;
    min-width: 200px; background: var(--surface);
    border: 1px solid var(--border); border-radius: var(--radius-sm);
    box-shadow: var(--shadow-lg);
    padding: 6px; z-index: 300;
    display: none;
    animation: dropIn .18s ease;
}
.dropdown-menu.show { display: block; }
@keyframes dropIn { from { opacity: 0; transform: translateY(-6px); } }
.dropdown-item {
    display: flex; align-items: center; gap: 10px;
    width: 100%; padding: 10px 12px;
    border: none; background: transparent; color: var(--text);
    font-size: .88rem; font-family: var(--font); cursor: pointer;
    border-radius: 8px; transition: background var(--transition);
}
.dropdown-item:hover { background: var(--surface-hover); }
.dropdown-item svg { width: 18px; height: 18px; color: var(--text-secondary); flex-shrink: 0; }
.dropdown-item.danger { color: var(--danger); }
.dropdown-item.danger svg { color: var(--danger); }
.dropdown-item.active { color: var(--accent); font-weight: 600; }
.dropdown-divider { height: 1px; background: var(--border); margin: 4px 0; }

.dropdown-category-submenu {
    display: none; padding-left: 12px;
}
.dropdown-category-submenu.show { display: block; }
.cat-option { font-size: .84rem; }

.color-palette {
    display: none; flex-wrap: wrap; gap: 8px;
    padding: 8px 12px;
}
.color-palette.show { display: flex; }
.color-swatch {
    width: 28px; height: 28px; border-radius: 50%; border: 2px solid transparent;
    cursor: pointer; transition: transform .15s, border-color var(--transition);
}
.color-swatch:hover { transform: scale(1.15); }
.color-swatch.active { border-color: var(--accent); }

/* Sort dropdown */
.sort-dropdown {
    position: fixed; z-index: 300;
}
.sort-dropdown.show { display: block; }

/* Editor Title */
.editor-title-row {
    padding: 20px 28px 0;
}
.editor-title-row input {
    width: 100%; border: none; outline: none;
    font-size: 1.65rem; font-weight: 700; font-family: var(--font);
    background: transparent; color: var(--text);
    letter-spacing: -.02em;
}
.editor-title-row input::placeholder { color: var(--text-tertiary); }

/* Attachments */
.attachments-area {
    padding: 12px 28px 0;
}
.attachments-area.hidden { display: none; }
.attachments-header {
    display: flex; align-items: center; gap: 6px;
    font-size: .78rem; font-weight: 600; color: var(--text-secondary);
    text-transform: uppercase; letter-spacing: .04em;
    margin-bottom: 8px;
}
.attachments-list {
    display: flex; flex-wrap: wrap; gap: 8px;
}
.attachment-chip {
    display: flex; align-items: center; gap: 8px;
    padding: 8px 12px; border-radius: 10px;
    background: var(--bg); border: 1px solid var(--border);
    font-size: .82rem; font-weight: 500;
    max-width: 220px;
    transition: background var(--transition);
    animation: chipIn .25s ease;
}
@keyframes chipIn { from { opacity: 0; transform: scale(.9); } }
.attachment-chip:hover { background: var(--surface-hover); }
.attachment-icon {
    width: 32px; height: 32px; border-radius: 8px;
    display: flex; align-items: center; justify-content: center;
    font-size: .7rem; font-weight: 700; color: #fff;
    flex-shrink: 0; text-transform: uppercase;
}
.attachment-info { flex: 1; min-width: 0; }
.attachment-name { display: block; font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.attachment-size { display: block; font-size: .72rem; color: var(--text-tertiary); }
.attachment-remove {
    width: 22px; height: 22px; border-radius: 50%;
    border: none; background: transparent;
    color: var(--text-tertiary); cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    transition: background var(--transition), color var(--transition);
    flex-shrink: 0;
}
.attachment-remove:hover { background: var(--danger-light); color: var(--danger); }
.attachment-remove svg { width: 14px; height: 14px; }
.attachment-img-preview {
    width: 40px; height: 40px; border-radius: 8px;
    object-fit: cover; flex-shrink: 0;
}

/* Editor Content */
.editor-content-wrapper {
    flex: 1; padding: 16px 28px; overflow-y: auto;
}
.editor-content-wrapper textarea {
    width: 100%; height: 100%;
    border: none; outline: none; resize: none;
    font-size: 1rem; line-height: 1.8;
    font-family: var(--font); color: var(--text);
    background: transparent;
}
.editor-content-wrapper textarea::placeholder { color: var(--text-tertiary); }

/* Format Toolbar */
.format-toolbar {
    border-top: 1px solid var(--border);
    padding: 8px 16px;
    display: flex; align-items: center; justify-content: space-between;
    flex-shrink: 0;
    background: var(--surface);
    transition: background var(--transition), border-color var(--transition);
}
.format-toolbar-scroll {
    display: flex; align-items: center; gap: 2px;
    overflow-x: auto;
}
.fmt-btn {
    width: 36px; height: 36px;
    display: flex; align-items: center; justify-content: center;
    border: none; background: transparent; color: var(--text-secondary);
    border-radius: 8px; cursor: pointer;
    font-size: .85rem; font-family: var(--font); font-weight: 600;
    transition: background var(--transition), color var(--transition);
    flex-shrink: 0;
}
.fmt-btn:hover { background: var(--surface-hover); color: var(--text); }
.fmt-btn:active { background: var(--surface-active); }
.fmt-divider { width: 1px; height: 20px; background: var(--border); margin: 0 4px; flex-shrink: 0; }
.editor-meta {
    display: flex; align-items: center; gap: 6px;
    font-size: .76rem; color: var(--text-tertiary);
    white-space: nowrap; flex-shrink: 0; padding-left: 12px;
}
.meta-dot { opacity: .5; }

/* ===== TEMPLATE MODAL ===== */
.modal-overlay {
    position: fixed; inset: 0; z-index: 250;
    background: rgba(0,0,0,.4); backdrop-filter: blur(4px);
    display: flex; align-items: center; justify-content: center;
    animation: fadeIn .2s;
}
.modal-overlay.hidden { display: none; }
@keyframes fadeIn { from { opacity: 0; } }
.modal-panel {
    background: var(--surface); border-radius: var(--radius);
    padding: 24px; width: 520px; max-width: 90vw;
    box-shadow: var(--shadow-lg);
    animation: modalIn .25s ease;
}
@keyframes modalIn { from { opacity: 0; transform: scale(.95) translateY(10px); } }
.modal-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 20px;
}
.modal-header h3 { font-size: 1.15rem; font-weight: 700; }
.template-grid {
    display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px;
}
.template-card {
    display: flex; flex-direction: column; align-items: center; gap: 10px;
    padding: 16px 10px; border-radius: var(--radius-sm);
    border: 2px solid var(--border); cursor: pointer;
    transition: border-color var(--transition), background var(--transition), transform .15s;
    text-align: center; font-size: .84rem; font-weight: 600;
}
.template-card:hover { border-color: var(--accent); background: var(--accent-light); transform: translateY(-2px); }
.template-preview {
    width: 100%; height: 60px; display: flex; flex-direction: column;
    gap: 5px; justify-content: center; padding: 0 8px;
}
.tline { height: 4px; border-radius: 2px; background: var(--border); }
.tline.short { width: 60%; }
.tline.accent { background: var(--accent); opacity: .5; }
.tcheck {
    height: 4px; border-radius: 2px; background: var(--border);
    position: relative; padding-left: 14px;
}
.tcheck::before {
    content: ''; position: absolute; left: 0; top: -2px;
    width: 8px; height: 8px; border: 2px solid var(--accent); border-radius: 3px;
}

/* ===== TOAST ===== */
.toast {
    position: fixed; bottom: 28px; left: 50%; transform: translateX(-50%) translateY(20px);
    background: #333; color: #fff; padding: 12px 24px;
    border-radius: 12px; font-size: .9rem; font-weight: 500;
    display: flex; align-items: center; gap: 16px;
    box-shadow: var(--shadow-lg);
    opacity: 0; transition: opacity .25s, transform .25s;
    z-index: 500;
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }
.toast.hidden { display: none; }
.toast-undo {
    border: none; background: transparent; color: var(--accent);
    font-weight: 700; font-family: var(--font); cursor: pointer;
    font-size: .9rem;
}
.toast-undo.hidden { display: none; }

/* ===== DOWNLOAD BANNER ===== */
.download-banner {
    background: linear-gradient(135deg, #0D1B2A 0%, #1B2838 50%, #0A1628 100%);
    color: #fff;
    padding: 0;
    display: flex;
    align-items: stretch;
    position: fixed;
    bottom: 0; left: 0; right: 0;
    z-index: 80;
    box-shadow: 0 -4px 30px rgba(0,0,0,.3);
    overflow: hidden;
    animation: bannerSlideUp .5s cubic-bezier(.32,1.2,.5,1);
    max-height: 80px;
    transition: max-height .3s, opacity .3s;
}
.download-banner.hidden { max-height: 0; opacity: 0; pointer-events: none; }
@keyframes bannerSlideUp { from { transform: translateY(100%); } }
.banner-glow {
    position: absolute; top: -40px; right: 20%; width: 200px; height: 120px;
    background: radial-gradient(ellipse, rgba(20,118,242,.25) 0%, transparent 70%);
    pointer-events: none;
}
.banner-content {
    display: flex; align-items: center; gap: 16px;
    padding: 14px 24px; flex: 1; position: relative; z-index: 1;
}
.banner-icon {
    width: 44px; height: 44px; border-radius: 12px;
    background: linear-gradient(135deg, #1476F2, #409CFF);
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0; box-shadow: 0 4px 12px rgba(20,118,242,.4);
}
.banner-icon svg { width: 24px; height: 24px; color: #fff; }
.banner-text { flex: 1; }
.banner-text h4 { font-size: .95rem; font-weight: 700; margin-bottom: 2px; }
.banner-text p { font-size: .78rem; opacity: .7; }
.banner-download-btn {
    display: flex; align-items: center; gap: 8px;
    padding: 10px 22px; border-radius: 24px; border: none;
    background: linear-gradient(135deg, #1476F2, #409CFF);
    color: #fff; font-size: .88rem; font-weight: 700;
    font-family: var(--font); cursor: pointer;
    transition: transform .15s, box-shadow .2s;
    box-shadow: 0 4px 16px rgba(20,118,242,.35);
    text-decoration: none; flex-shrink: 0;
}
.banner-download-btn:hover { transform: scale(1.04); box-shadow: 0 6px 24px rgba(20,118,242,.45); }
.banner-download-btn:active { transform: scale(.96); }
.banner-download-btn svg { width: 18px; height: 18px; }
.banner-close {
    width: 36px; height: 36px; display: flex; align-items: center; justify-content: center;
    border: none; background: rgba(255,255,255,.1); color: rgba(255,255,255,.7);
    border-radius: 50%; cursor: pointer; margin-right: 12px; flex-shrink: 0;
    transition: background .2s;
}
.banner-close:hover { background: rgba(255,255,255,.2); }
.banner-close svg { width: 18px; height: 18px; }

/* Add bottom padding when banner is visible */
body.banner-visible .fab { bottom: 108px; }
body.banner-visible .fab-menu { bottom: 180px; }

/* ===== RESPONSIVE ===== */
@media (max-width: 900px) {
    .sidebar { position: fixed; left: 0; top: 0; height: 100%; transform: translateX(-100%); z-index: 150; box-shadow: var(--shadow-lg); }
    .sidebar.open { transform: translateX(0); }
    .sidebar-close-btn { display: flex; }
    #hamburgerBtn { display: flex; }
    .editor-panel { margin: 0; border-radius: 0; }
}

@media (max-width: 600px) {
    .notes-container.grid-view { grid-template-columns: repeat(auto-fill, minmax(160px, 1fr)); gap: 10px; padding: 14px; }
    .topbar { padding: 0 12px; }
    .topbar-center { max-width: unset; }
    .editor-title-row { padding: 16px 18px 0; }
    .editor-content-wrapper { padding: 12px 18px; }
    .format-toolbar { padding: 6px 10px; }
    .template-grid { grid-template-columns: repeat(2, 1fr); }
    .banner-content { padding: 10px 14px; }
    .banner-text h4 { font-size: .85rem; }
    .banner-download-btn { padding: 8px 16px; font-size: .82rem; }
}
